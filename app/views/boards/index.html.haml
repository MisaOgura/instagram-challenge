%h1 Your Boards
- if @boards.any?
  %ul
    - @boards.each do |board|
      %li
        %h2= link_to "#{board.name}", board_path(board)
- else
  %h2 No board has been added

= link_to 'Create new board', new_board_path
